server {
    listen 80;
    listen [::]:80;
    server_name wevault.online;
    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    # Serve no unencrypted installer while HTTPS is being provisioned.
    location / { return 503; }
}
